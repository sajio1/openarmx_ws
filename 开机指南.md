# OpenArmX 开机操作指南

从开机到实时控制的完整流程。

---

## 1. 硬件准备

1. 确保机械臂电源已连接
2. 确保 CAN 线已连接到电脑（USB-CAN 适配器）
3. **重要**：确保机械臂处于安全位置，周围无障碍物

---

## 2. 启动 CAN 接口

打开终端，执行：

```bash
cd ~/Desktop/openarmx_ws/src/openarmx_motor_manager/scripts
python3 en_all_can.py
```

成功后会显示类似：
```
找到 6 个真实CAN接口
...
```

**提示**：可以先检查 CAN 是否已启用：
```bash
ip link show | grep can
```
如果显示 `state UP`，则已启用，可跳过此步骤。

---

## 3. 检查电机状态

确认电机连接正常：

```bash
cd ~/Desktop/openarmx_ws/src/openarmx_motor_manager/scripts
python3 check_motor_status.py
```

脚本会检查所有检测到的 CAN 配对组。**只需关注有响应的 Robot**。

示例输出（单机器人在 can2-can3）：
```
检测到可用的CAN通道: ['can0', 'can1', 'can2', 'can3', 'can4', 'can5']
找到 3 个有效的配对组

【Robot #1】CAN通道: can0-can1
右臂 |  1 | ✗ 无响应          ← 忽略，没有连接机器人
...

【Robot #2】CAN通道: can2-can3   ← 你的机器人在这里！
右臂 |  1 |  0.027 | ... | ✓ 正常
右臂 |  2 |  0.050 | ... | ✓ 正常
...
左臂 |  1 |  0.005 | ... | ✓ 正常
...

【Robot #3】CAN通道: can4-can5
右臂 |  1 | ✗ 无响应          ← 忽略，没有连接机器人
...
```

**如果你的机器人也显示 "无响应"**：
- 检查电源是否开启
- 检查 CAN 线连接
- 重新运行 `en_all_can.py`

---

## 4. 设置零点（可选）

如果当前位置需要设为零点：

```bash
cd ~/Desktop/openarmx_ws/src/openarmx_motor_manager/scripts
python3 set_motor_zero.py
```

**注意**：此操作将当前物理位置记录为零点，不会移动机械臂。

---

## 5. 启动实时控制

```bash
cd ~/Desktop/openarmx_ws/src/openarmx_motor_manager/scripts
python3 realtime_control.py
```

脚本会**自动扫描**所有 CAN 配对，找到有响应的机器人并连接。

### 操作步骤

1. **连接机器人** - 点击按钮，等待显示 "已连接"
2. **开始控制** - 点击按钮，进入实时控制模式
3. **拖动滑块** - 机械臂实时跟随移动
4. **全部归零** - 滑块归零（⚠️ 当前版本无阻尼，谨慎使用）
5. **停止控制** - 停止发送控制指令
6. **断开连接** - 安全断开

---

## 6. 关闭 CAN 接口（可选）

结束使用后：

```bash
cd ~/Desktop/openarmx_ws/src/openarmx_motor_manager/scripts
python3 dis_all_can.py
```

---

## 快速命令汇总

```bash
# 进入脚本目录
cd ~/Desktop/openarmx_ws/src/openarmx_motor_manager/scripts

# 启动 CAN
python3 en_all_can.py

# 检查状态
python3 check_motor_status.py

# 设置零点
python3 set_motor_zero.py

# 实时控制
python3 realtime_control.py

# 关闭 CAN
python3 dis_all_can.py
```

---

## 常见问题

| 问题 | 解决方案 |
|------|----------|
| CAN 接口无法启动 | 检查 USB-CAN 适配器是否插好 |
| 电机无响应 | 检查电源、CAN 线连接 |
| 位置读数异常 | 运行 `set_motor_zero.py` 重新校准 |
| 控制不流畅 | 检查 CAN 总线负载，减少其他程序占用 |

---

## 安全提示

- 首次控制时，从小幅度开始
- 保持急停按钮在手边
- 归零操作前确保路径无障碍
- 断电前先停止控制并断开连接
